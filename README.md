# separatrix.ai

Public site for Separatrix. One page, hand-built, no dependencies.

- `index.html` — the site (inline CSS/JS/SVG)
- `figure.py` — generates the phase-portrait trajectories inlined in index.html
- Hosted on GitHub Pages; DNS on Cloudflare.
